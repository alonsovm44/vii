# Vii 1.4 TODO List

## Vision
Python syntax, C semantics. One language for beginners and experts.
Beginners write simple code, experts add annotations for control.

---

## Phase 0: Bundling ✅ (Primary Distribution Method)
- [x] Remove C codegen - bundling is simpler and more reliable
- [x] To make standalone executables, bundle the code + interpreter on a binary file
- [x] Add `vii script.vii --bundle [app | app.exe]` to make final binary
- [x] Self-extracting bundle detection at startup
- [x] Bundle format: [interpreter] + [script] + [footer: offset + magic "VIIBUNDL"]
- [x] Single source of truth - one interpreter, no C/Vii drift

## Phase 1: Core Type System (Critical)

### Integer Types ✅
- [x] Add `i8`, `i16`, `i32`, `i64` signed integers
- [x] Add `u8`, `u16`, `u32`, `u64` unsigned integers
- [x] Add `f32`, `f64` floating point
- [x] Update lexer to recognize type keywords
- [x] Update parser for type annotations syntax: `x i32 = 5`
- [x] Update codegen to emit correct C types

### Functions with Types ✅
- [x] Functions support return values for new types
- [x] Function arguments support new types  
- [x] Error if type and value types don't match 

[DONE] on 26 of April 2026

### Type Inference
- [ ] Implement basic inference: `x = 5` → `i32`
- [ ] Implement float inference: `x = 5.0` → `f64`
- [ ] Update symbol table to track inferred types
- [ ] Add type conflict detection

### Explicit Casting
- [ ] Add `as` keyword: `x as u32`
- [ ] Implement cast codegen
- [ ] Add cast safety warnings

---

## Phase 2: Memory Model (Critical)

### Stack Allocation
- [ ] Add `stack.alloc` builtin
- [ ] Implement fixed arrays: `[N]T` syntax
- [ ] Add array bounds checking (debug mode)
- [ ] Generate alloca or VLAs in C

### Heap Allocation
- [ ] Add `heap.alloc` builtin
- [ ] Add `free` builtin
- [ ] Implement pointer type: `*T` or `ptr T`
- [ ] Add dereference operator: `*ptr` or `ptr.deref()`
- [ ] Add address-of operator: `&x`

### Custom Allocators
- [ ] Pass allocator to functions: `fn foo allocator -> ...`
- [ ] Multiple arena support: `arena "name" size`
- [ ] `arena_reset` for named arenas

---

## Phase 3: Low-Level Primitives (Critical)

### sizeof / alignof
- [ ] `sizeof T` operator
- [ ] `sizeof expr` for values
- [ ] `alignof T` operator
- [ ] Update codegen for compile-time constants

### Pointer Arithmetic
- [ ] `ptr.add offset` method
- [ ] `ptr.sub offset` method
- [ ] Pointer comparison operators

### Bit Operations
- [ ] Add bitwise NOT (`~` or `not`)
- [ ] Add bit extraction: `x.bits start end`
- [ ] Add bit setting: `x.set_bit n val`

---

## Phase 4: Unsafe Blocks (Critical)

### Unsafe Syntax
- [ ] Add `unsafe` block keywords, no end word, just indentation to measure scope
- [ ] Disable bounds checks inside unsafe
- [ ] Allow raw pointer arithmetic
- [ ] Allow raw dereference

### Unsafe Operations
- [ ] `ptr.raw` to get void* equivalent
- [ ] `mem.read addr T` for typed reads
- [ ] `mem.write addr val T` for typed writes
- [ ] `mem.copy dst src len` for bulk copy

---

## Phase 5: Structs and Layout (High)

### Struct Syntax
- [ ] Basic struct (called thing): `thing Name | field T, ... end`
- [ ] Struct literals: `Name { field: val, ... }`
- [ ] Field access: `s.field` or `s at "field"`

### Advanced Struct
- [ ] `packed` attribute: `thing packed | ...`
- [ ] `aligned N` attribute: `struct aligned 16 | ...`
- [ ] `offsetof Struct field` operator
- [ ] Anonymous structs

### Union
- [ ] `union Name | field T, ... end`
- [ ] Tagged unions (discriminated)

---

## Phase 6: Inline Assembly (High)

### Basic ASM
- [ ] `asm` block, indentation for scope
- [ ] AT&T syntax support
- [ ] Register constraints
- [ ] Clobber list

### Advanced ASM
- [ ] Intel syntax option
- [ ] Multiple architectures (x86, ARM, RISC-V)
- [ ] WASM inline assembly
- [ ] Assembly templates

---

## Phase 7: Systems Programming (High)

### Freestanding Mode
- [ ] `--freestanding` flag
- [ ] Remove libc dependencies
- [ ] Custom `_start` entry point
- [ ] Syscall wrappers

### No-Std Mode
- [ ] `--no-std` flag
- [ ] Disable arena allocator
- [ ] Disable dynamic lists/dicts
- [ ] Pure manual memory mode

### Linker Control
- [ ] `--script linker.ld` support
- [ ] Section attributes: `section ".data"`
- [ ] Custom entry points

---

## Phase 8: FFI and C Interop (High)

### External Declarations
- [ ] `extern "name" retT (paramT, ...)`
- [ ] C header parsing (optional)
- [ ] Varargs support: `extern "printf" i32 (ptr u8, ...)`

### Calling Conventions
- [ ] `cdecl`, `stdcall`, `fastcall` attributes
- [ ] System V AMD64 ABI compliance
- [ ] Windows x64 ABI compliance

---

## Phase 9: Developer Experience (High)

### Error Messages
- [ ] Source line display with caret
- [ ] Type mismatch hints
- [ ] Suggest fixes for common errors

### Introspection
- [ ] `--emit-c` flag to show generated C
- [ ] `--emit-asm` flag to show assembly
- [ ] `--emit-llvm` (future)

### REPL Improvements
- [ ] Multi-line input
- [ ] Type display: `:t expr` shows type
- [ ] Memory inspection

---

## Phase 10: Tooling (Medium)

### Type Checker
- [ ] `--check` flag for validation only
- [ ] Warn on implicit conversions
- [ ] Strict mode: `--strict`

### Formatter
- [ ] `vii fmt` command
- [ ] Configurable style
- [ ] Format on save (LSP)

### LSP
- [ ] Autocomplete
- [ ] Go to definition
- [ ] Type hover
- [ ] Rename symbol

---

## Deferred / Nice-to-Have

- [ ] defer/finally blocks
- [ ] switch/match expressions
- [ ] String interpolation
- [ ] Module system (namespaced imports)
- [ ] Package manager
- [ ] Cross-compilation
- [ ] Interrupt handlers
- [ ] enum types
- [ ] Documentation generator

---

## Quick Wins (Start Here)

1. **Integer types** - Add keywords, parser, codegen
2. **Type annotations** - `x i32 = 5` syntax
3. **sizeof** - Essential for low-level work
4. **Fixed arrays** - `[N]T` syntax
5. **unsafe blocks** - Escape hatch for experts
6. **--emit-c** - Transparency for learning

---

## Definition of Done

For each feature:
- [ ] Lexer support
- [ ] Parser support
- [ ] AST representation
- [ ] Type checking
- [ ] Codegen (C output)
- [ ] Tests
- [ ] Documentation with examples
- [ ] Error messages

---

## Notes

- Keep beginner syntax working unchanged
- All new features are opt-in via annotations
- Transparency: always can see generated C
- Safety: debug mode checks everything
- Speed: release mode removes checks
