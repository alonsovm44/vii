paste "vii.vii"

do enable_ansi_colors 
  IF WIN
    handle h getStdHandle STD_error_handler
    dword mode = 0
    if (getConsoleMode h mode)
    


do report_error filename->ref src->ref pos->num line->num fmt->ref 
